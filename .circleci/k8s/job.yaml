apiVersion: batch/v1
kind: Job
metadata:
  name: prices-predict-job
  namespace: prices-predict
spec:
  template:
    metadata:
      labels:
        app: prices-predict
        role: prices-predict-job
    spec:
      securityContext:
        runAsGroup: 1000
        runAsUser: 1000
      restartPolicy: OnFailure
      containers:
        - name: prices-predict-job
          image: ovimihai/cdond-ml-microservice
          command:
            - /opt/venv/bin/python
          args:
            - app.py
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 256Mi
              cpu: 200m